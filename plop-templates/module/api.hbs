import api from '@/api'
import type { BasicPageParams } from '@/api/model/baseModel'
import type { {{ pascalCase moduleName }}ListItem, {{ pascalCase moduleName }}PageListGetResultModel, {{ pascalCase moduleName }}Params } from '@/api/modules/{{#if relativePath}}{{ relativePath }}/{{/if}}model/{{ snakeCase moduleName }}Model'

// 前缀 变量
const prefix = '/{{#if relativePath}}{{ relativePath }}/{{/if}}{{ snakeCase moduleName }}'
export function getListRole(params: BasicPageParams & {{ pascalCase moduleName }}Params) {
  return api.get<{{ pascalCase moduleName }}PageListGetResultModel>({
    url: `${prefix}/list`,
    params,
  })
}
export function detailRole(id: string | number) {
  return api.get<{{ pascalCase moduleName }}ListItem>({
    url: `${prefix}/detail`,
    params: {
      id,
    },
  })
}

export function createRole(data: any) {
  return api.post<{{ pascalCase moduleName }}ListItem>({
    url: `${prefix}/create`,
    data,
  })
}

export function editRole(data: any) {
  return api.post<void>({
    url: `${prefix}/edit`,
    data,
  })
}

export function deleteRole(id: string) {
  return api.post<void>({
    url: `${prefix}/delete`,
    data: {
      id,
    },
  })
}
export default {
  list: getListRole,
  detail: detailRole,
  create: createRole,
  edit: editRole,
  delete: deleteRole,
}
